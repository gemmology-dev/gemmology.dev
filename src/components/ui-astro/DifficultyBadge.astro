---
/**
 * DifficultyBadge - Astro version of the React DifficultyBadge component
 * Specialized badge for indicating content difficulty levels.
 */

type DifficultyLevel = 'beginner' | 'intermediate' | 'advanced';
type DifficultySize = 'sm' | 'md';

interface Props {
  level: DifficultyLevel;
  size?: DifficultySize;
  class?: string;
}

const { level, size = 'sm', class: className } = Astro.props;

const levelStyles: Record<DifficultyLevel, string> = {
  beginner:
    'bg-emerald-100 text-emerald-700 dark:bg-emerald-400/10 dark:text-emerald-300 dark:border dark:border-emerald-400/20',
  intermediate:
    'bg-amber-100 text-amber-700 dark:bg-amber-400/10 dark:text-amber-300 dark:border dark:border-amber-400/20',
  advanced: 'bg-red-100 text-red-700 dark:bg-red-400/10 dark:text-red-300 dark:border dark:border-red-400/20',
};

const sizeStyles: Record<DifficultySize, string> = {
  sm: 'text-xs px-2 py-0.5',
  md: 'text-sm px-2.5 py-1',
};

const classes = [
  'inline-block rounded-full font-medium capitalize',
  levelStyles[level],
  sizeStyles[size],
  className,
].filter(Boolean).join(' ');
---

<span class={classes}>
  {level}
</span>
