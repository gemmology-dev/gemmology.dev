---
interface NavLink {
  label: string;
  href: string;
}

interface Props {
  prev?: NavLink;
  next?: NavLink;
}

const { prev, next } = Astro.props;
---

<nav class="page-nav" aria-label="Page navigation">
  {prev ? (
    <a href={prev.href} class="nav-link prev">
      <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M11 17l-5-5m0 0l5-5m-5 5h12" />
      </svg>
      <span>{prev.label}</span>
    </a>
  ) : <span class="nav-spacer" />}

  {next && (
    <a href={next.href} class="nav-link next">
      <span>{next.label}</span>
      <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M13 7l5 5m0 0l-5 5m5-5H6" />
      </svg>
    </a>
  )}
</nav>

<style>
  .page-nav {
    @apply flex justify-between items-center mt-16 pt-8;
    @apply border-t border-slate-200 dark:border-coffee-border;
  }

  .nav-link {
    @apply flex items-center gap-2 px-5 py-3 rounded-lg;
    @apply text-sm font-medium transition-all;
  }

  .nav-link.prev {
    @apply text-slate-600 border border-slate-200;
    @apply hover:border-crystal-300 hover:text-crystal-700 hover:bg-crystal-50;
    @apply focus-visible:outline-2 focus-visible:outline-crystal-500 focus-visible:outline-offset-2;
    @apply dark:text-cream-secondary dark:border-coffee-border;
    @apply dark:hover:border-crystal-400/40 dark:hover:text-crystal-300 dark:hover:bg-crystal-400/10;
    @apply dark:focus-visible:outline-crystal-400;
  }

  .nav-link.next {
    @apply text-white bg-crystal-700;
    @apply hover:bg-crystal-800 hover:shadow-lg hover:shadow-crystal-500/25;
    @apply focus-visible:outline-2 focus-visible:outline-white focus-visible:outline-offset-2;
    /* Solid primary surface: dark mode gets a border to satisfy the
       no-shadow-without-border gate, plus the standard crystal-600/500
       primary-button token pairing. */
    @apply dark:bg-crystal-600 dark:border dark:border-crystal-400/30;
    @apply dark:hover:bg-crystal-500 dark:hover:shadow-none;
  }

  .icon {
    @apply w-4 h-4;
  }
</style>
