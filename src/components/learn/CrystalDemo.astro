---
/**
 * CrystalDemo - Shows a CDL expression with link to playground
 * Updated to use Astro UI components
 */
import Card from '../ui-astro/Card.astro';
import Button from '../ui-astro/Button.astro';
import { PLAYGROUND_ENABLED } from '../../lib/feature-flags';

interface Props {
  cdl: string;
  caption?: string;
  interactive?: boolean;
}

const { cdl, caption, interactive = true } = Astro.props;

// For now, link to the playground. In the future, this could embed a 3D viewer.
const playgroundUrl = `/playground?cdl=${encodeURIComponent(cdl)}`;
---

<figure class="my-8">
  <Card padding="md" class="bg-slate-100 border-slate-200 dark:bg-coffee-raised dark:border-coffee-border flex flex-col items-center justify-center min-h-[200px]">
    <div class="text-center">
      <code class="block text-sm font-mono text-slate-600 dark:text-cream-secondary mb-4 bg-white dark:bg-coffee-sunk px-3 py-2 rounded-lg border border-slate-200 dark:border-coffee-border">{cdl}</code>
      {PLAYGROUND_ENABLED && (
        <Button href={playgroundUrl} variant="primary" size="md">
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z" />
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          View in Playground
        </Button>
      )}
    </div>
  </Card>
  {caption && (
    <figcaption class="text-center text-sm text-slate-600 dark:text-cream-secondary mt-2">
      {caption}
    </figcaption>
  )}
</figure>
