---
interface Props {
  headers: string[];
  rows: string[][];
  caption?: string;
}

const { headers, rows, caption } = Astro.props;
---

<div class="overflow-x-auto my-6">
  <table class="min-w-full divide-y divide-slate-200">
    {caption && (
      <caption class="text-sm text-slate-500 mb-2 text-left">{caption}</caption>
    )}
    <thead class="bg-slate-50">
      <tr>
        {headers.map(header => (
          <th class="px-4 py-3 text-left text-xs font-semibold text-slate-600 uppercase tracking-wider">
            {header}
          </th>
        ))}
      </tr>
    </thead>
    <tbody class="bg-white divide-y divide-slate-200">
      {rows.map(row => (
        <tr class="hover:bg-slate-50 transition-colors">
          {row.map((cell, i) => (
            <td class={`px-4 py-3 text-sm ${i === 0 ? 'font-medium text-slate-900' : 'text-slate-600'}`}>
              {cell}
            </td>
          ))}
        </tr>
      ))}
    </tbody>
  </table>
</div>
