---
import { DocsLayout, CodeBlock, APISignature, PropsTable } from '../../../components/docs';

const geometryAttrs = [
  { name: 'vertices', type: 'np.ndarray', description: 'Nx3 array of vertex coordinates' },
  { name: 'faces', type: 'list[list[int]]', description: 'List of faces (each face is a list of vertex indices)' },
  { name: 'face_normals', type: 'list[np.ndarray]', description: 'Normal vectors for each face' },
  { name: 'face_forms', type: 'list[int]', description: 'Form index for each face (for colour-by-form rendering)' },
  { name: 'face_millers', type: 'list[tuple[int, int, int]]', description: 'Miller indices for each face' },
  { name: 'forms', type: 'list[CrystalForm]', description: 'Original form definitions from CDL' },
  { name: 'component_ids', type: 'list[int] | None', description: 'Component ID for each face, for twins/aggregates' },
  { name: 'twin_metadata', type: 'TwinMetadata | None', description: 'Twin metadata if geometry is twinned' },
  { name: 'is_amorphous', type: 'bool', description: 'Whether this is an amorphous (non-crystalline) geometry (default False)' },
  { name: 'aggregate_metadata', type: 'AggregateMetadata | None', description: 'Aggregate metadata if geometry was generated via generate_aggregate()' },
];

const latticeAttrs = [
  { name: 'a', type: 'float', description: 'Unit cell parameter a (default 1.0)' },
  { name: 'b', type: 'float', description: 'Unit cell parameter b (default 1.0)' },
  { name: 'c', type: 'float', description: 'Unit cell parameter c (default 1.0)' },
  { name: 'alpha', type: 'float', description: 'Angle alpha, in radians (default π/2)' },
  { name: 'beta', type: 'float', description: 'Angle beta, in radians (default π/2)' },
  { name: 'gamma', type: 'float', description: 'Angle gamma, in radians (default π/2)' },
];
---

<DocsLayout
  title="crystal-geometry API"
  description="API reference for the crystal-geometry package - 3D geometry generation using half-space intersection."
  showToc
>
    <article class="prose prose-slate max-w-none
                    dark:prose-headings:text-cream-primary
                    dark:prose-p:text-cream-secondary
                    dark:prose-li:text-cream-secondary
                    dark:prose-strong:text-cream-primary
                    dark:prose-a:text-crystal-400 dark:hover:prose-a:text-crystal-300 dark:prose-a:decoration-crystal-400/30 dark:hover:prose-a:decoration-gold">
      <h1>crystal-geometry</h1>

      <p class="lead">
        Generate 3D crystal geometry from CDL descriptions using half-space intersection
        and crystallographic symmetry operations. Version 2.0.0.
      </p>

      <CodeBlock code="pip install gemmology-crystal-geometry" lang="bash" />

      <h2>Core Functions</h2>

      <APISignature
        name="cdl_to_geometry"
        signature="cdl_to_geometry(description: CrystalDescription) → CrystalGeometry"
      />

      <p>Convert a parsed CDL description into 3D geometry. Uses <code>desc.flat_forms()</code>
      internally to handle <code>FormGroup</code> nodes.</p>

      <CodeBlock
        lang="python"
        code={`from cdl_parser import parse_cdl
from crystal_geometry import cdl_to_geometry

desc = parse_cdl("cubic[m3m]:{111}@1.0 + {100}@1.3")
geom = cdl_to_geometry(desc)

print(f"Vertices: {len(geom.vertices)}")  # 24
print(f"Faces: {len(geom.faces)}")        # 14`}
      />

      <p><strong>Parameters:</strong></p>
      <ul>
        <li><code>description</code> - Parsed <code>CrystalDescription</code> from cdl-parser</li>
      </ul>

      <p><strong>Returns:</strong> <code>CrystalGeometry</code> object with vertices, faces, and normals</p>

      <APISignature
        name="cdl_string_to_geometry"
        signature="cdl_string_to_geometry(cdl_string: str) → CrystalGeometry"
      />

      <p>Convenience function that parses a CDL string and generates geometry in one call.</p>

      <CodeBlock
        lang="python"
        code={`from crystal_geometry import cdl_string_to_geometry

geom = cdl_string_to_geometry("cubic[m3m]:{111}")
print(len(geom.vertices))  # 6
print(len(geom.faces))     # 8`}
      />

      <APISignature
        name="halfspace_intersection_3d"
        signature="halfspace_intersection_3d(normals, distances, interior_point=None) → ndarray | None"
      />

      <p>Compute the vertices of the convex polyhedron defined by half-space intersection
      (each half-space <code>normal · x ≤ distance</code>). Returns an Nx3 vertex array,
      or <code>None</code> if the intersection is empty/unbounded. Use
      <code>compute_face_vertices()</code> to derive the ordered face index list for
      each plane from the resulting vertices.</p>

      <APISignature
        name="compute_face_vertices"
        signature="compute_face_vertices(vertices, normal, distance, tolerance=1e-6) → list[int]"
      />

      <p>Find the vertex indices lying on a single face plane, ordered counter-clockwise
      when viewed from outside. Called once per face normal/distance pair to build up
      a polyhedron's <code>faces</code> list from the vertices returned by
      <code>halfspace_intersection_3d</code>.</p>

      <h2>Convenience Constructors</h2>

      <CodeBlock
        lang="python"
        code={`from crystal_geometry import (
    create_octahedron,
    create_cube,
    create_dodecahedron,
    create_truncated_octahedron,
)

# Create common crystal forms directly
geom = create_octahedron()       # {111} in m3m
geom = create_cube()             # {100} in m3m
geom = create_dodecahedron()     # {110} in m3m
geom = create_truncated_octahedron()  # {111} + {100}`}
      />

      <h2>Symmetry Operations</h2>

      <APISignature
        name="get_point_group_operations"
        signature="get_point_group_operations(point_group: str) → list[np.ndarray]"
      />

      <p>Get the 3x3 rotation/reflection matrices for a crystallographic point group.</p>

      <CodeBlock
        lang="python"
        code={`from crystal_geometry import get_point_group_operations

matrices = get_point_group_operations("m3m")
print(len(matrices))  # 48 operations

matrices = get_point_group_operations("-3m")
print(len(matrices))  # 12 operations`}
      />

      <APISignature
        name="generate_equivalent_faces"
        signature="generate_equivalent_faces(miller, point_group: str) → list[ndarray]"
      />

      <p>Generate all symmetry-equivalent face normals from a single Miller index.</p>

      <APISignature
        name="miller_to_normal"
        signature="miller_to_normal(h, k, l, lattice) → ndarray"
      />

      <p>Convert Miller indices to a Cartesian normal vector using lattice parameters.</p>

      <APISignature
        name="get_lattice_for_system"
        signature="get_lattice_for_system(system: str) → LatticeParams"
      />

      <p>Get default lattice parameters for a crystal system.</p>

      <h2>Modification Functions</h2>

      <APISignature
        name="apply_modifications"
        signature="apply_modifications(vertices, modifications) → ndarray"
      />

      <p>Apply morphological modifications (elongate, flatten, taper, twist) to vertices.
      Each modifier takes a <code>params</code> dict (matching the parsed CDL
      <code>Modification.params</code>), not individual keyword arguments.</p>

      <CodeBlock
        lang="python"
        code={`from crystal_geometry import apply_elongation, apply_flatten, apply_taper, apply_twist

# Elongate along c-axis
vertices = apply_elongation(vertices, {'axis': 'c', 'ratio': 1.5})

# Flatten along c-axis
vertices = apply_flatten(vertices, {'axis': 'c', 'ratio': 0.5})

# Taper toward one end
vertices = apply_taper(vertices, {'direction': '+c', 'factor': 0.3})

# Twist around an axis
vertices = apply_twist(vertices, {'axis': 'c', 'angle': 30})`}
      />

      <h2>Amorphous &amp; Aggregate Geometry</h2>

      <APISignature
        name="generate_amorphous_shape"
        signature="generate_amorphous_shape(shape: str, radius: float = 1.0, seed: int = 42) → CrystalGeometry"
      />

      <p>Generate non-crystalline geometry (e.g. <code>massive</code>, <code>botryoidal</code>,
      <code>reniform</code>, <code>stalactitic</code>, <code>mammillary</code>, <code>nodular</code>,
      <code>conchoidal</code>) for CDL <code>amorphous[...]</code> descriptions. The returned
      <code>CrystalGeometry</code> has <code>is_amorphous=True</code>. <code>seed</code> makes the
      procedural mesh deterministic.</p>

      <APISignature
        name="generate_aggregate"
        signature="generate_aggregate(base_geometry: CrystalGeometry, arrangement: str, count: int, spacing: float | None = None, orientation: str | None = None, seed: int = 42) → CrystalGeometry"
      />

      <p>Instance a base geometry into a crystal aggregate (<code>parallel</code>, <code>random</code>,
      <code>radial</code>, <code>epitaxial</code>, <code>druse</code>, <code>cluster</code>
      arrangements), matching CDL's <code>~</code> aggregate operator. The result's
      <code>aggregate_metadata</code> records the arrangement, instance count, spacing, and
      orientation; <code>component_ids</code> attributes each face back to its instance.
      <code>count</code> is capped by <code>AGGREGATE_MAX_INSTANCES</code>.</p>

      <CodeBlock
        lang="python"
        code={`from crystal_geometry import generate_amorphous_shape, generate_aggregate, create_octahedron

# Botryoidal amorphous mass (e.g. precious opal)
geom = generate_amorphous_shape("botryoidal", radius=1.0)
print(geom.is_amorphous)  # True

# Cluster of octahedra
base = create_octahedron()
cluster = generate_aggregate(base, arrangement="cluster", count=6)
print(cluster.aggregate_metadata.arrangement)  # 'cluster'
print(cluster.aggregate_metadata.n_instances)  # 6`}
      />

      <h2>Twin System</h2>

      <CodeBlock
        lang="python"
        code={`from crystal_geometry import (
    get_twin_law,
    list_twin_laws,
    get_gemstone_twins,
    TwinLaw,
)

# List all available twin laws (14 total)
laws = list_twin_laws()
print(laws)  # ['albite', 'baveno', 'brazil', 'carlsbad', 'dauphine', ...]

# Get a specific twin law (note: 'spinel_law', not 'spinel')
law = get_twin_law("spinel_law")
print(law.name)      # 'Spinel Law (Macle)'
print(law.axis)      # array([...]) - twin rotation/reflection axis
print(law.angle)     # 180.0

# Get all twin laws for a specific gemstone
twins = get_gemstone_twins("quartz")
print(twins)  # ['brazil', 'dauphine', 'japan']`}
      />

      <h2>Habit System</h2>

      <CodeBlock
        lang="python"
        code={`from crystal_geometry import (
    get_habit,
    list_habits,
    get_gemstone_habits,
    CrystalHabit,
)

# List all registered habits (15 total)
habits = list_habits()
print(habits)  # ['barrel', 'cube', 'dodecahedron', 'feldspar_tabular', ...]

# Get a specific habit - CrystalHabit exposes vertices/faces directly
# (there is no .generate() method)
habit = get_habit("hexagonal_prism", scale=1.0)
print(habit.name)            # 'Hexagonal Prism'
print(habit.vertices.shape)  # (12, 3)
print(len(habit.faces))      # 8

# Get habits for a specific gemstone
gem_habits = get_gemstone_habits("diamond")
print(gem_habits)  # ['octahedron', 'dodecahedron', 'cube']`}
      />

      <h2>Classes</h2>

      <h3 id="CrystalGeometry"><code>CrystalGeometry</code></h3>

      <p>Container for 3D crystal geometry data.</p>

      <PropsTable
        columns={[
          { key: 'name', header: 'Attribute', mono: true },
          { key: 'type', header: 'Type', mono: true },
          { key: 'description', header: 'Description' }
        ]}
        rows={geometryAttrs}
      />

      <h3 id="LatticeParams"><code>LatticeParams</code></h3>

      <p>Unit cell lattice parameters.</p>

      <PropsTable
        columns={[
          { key: 'name', header: 'Attribute', mono: true },
          { key: 'type', header: 'Type', mono: true },
          { key: 'description', header: 'Description' }
        ]}
        rows={latticeAttrs}
      />

      <h2>Backend Acceleration</h2>

      <CodeBlock
        lang="python"
        code={`from crystal_geometry import get_backend, get_backend_info

# Check which backend is active
print(get_backend())      # 'native' or 'python'
print(get_backend_info()) # Detailed backend information`}
      />
    </article>
</DocsLayout>

<style>
  :global([data-theme='dark'] .prose code) {
    background-color: #332619; /* coffee-raised2 */
    color: #ede3d5; /* cream-primary */
  }
  :global([data-theme='dark'] .prose ul > li) {
    border-bottom-color: #362b23; /* coffee-border */
  }
  :global([data-theme='dark'] .prose ul ul) {
    border-left-color: #362b23; /* coffee-border */
  }
  :global([data-theme='dark'] .prose ol > li::before) {
    color: #7dd3fc; /* crystal-300 */
    background-color: rgba(56, 189, 248, 0.1); /* crystal-400/10 */
  }
</style>
