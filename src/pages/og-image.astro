---
/**
 * OG Image Template
 * Standalone HTML page that renders to a 1200x630 social card
 * Convert to image using: npx playwright screenshot or puppeteer
 */
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=1200, height=630">
  <title>gemmology.dev OG Image</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@500&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      /* Crystal palette from tailwind config */
      --crystal-50: #f0f9ff;
      --crystal-100: #e0f2fe;
      --crystal-200: #bae6fd;
      --crystal-300: #7dd3fc;
      --crystal-400: #38bdf8;
      --crystal-500: #0ea5e9;
      --crystal-600: #0284c7;
      --crystal-700: #0369a1;
      --crystal-800: #075985;
      --crystal-900: #0c4a6e;

      /* Slate palette */
      --slate-50: #f8fafc;
      --slate-100: #f1f5f9;
      --slate-200: #e2e8f0;
      --slate-300: #cbd5e1;
      --slate-400: #94a3b8;
      --slate-500: #64748b;
      --slate-600: #475569;
      --slate-700: #334155;
      --slate-800: #1e293b;
      --slate-900: #0f172a;
    }

    body {
      font-family: 'Inter', system-ui, sans-serif;
      background: linear-gradient(135deg, var(--crystal-50) 0%, white 50%, var(--slate-50) 100%);
      width: 1200px;
      height: 630px;
      overflow: hidden;
      position: relative;
    }

    /* Decorative background elements - matching hero section */
    .bg-decoration {
      position: absolute;
      border-radius: 50%;
      filter: blur(80px);
      z-index: 0;
    }

    .bg-decoration-1 {
      width: 400px;
      height: 400px;
      background: var(--crystal-200);
      opacity: 0.4;
      top: -100px;
      left: -100px;
    }

    .bg-decoration-2 {
      width: 500px;
      height: 500px;
      background: var(--crystal-100);
      opacity: 0.3;
      bottom: -150px;
      right: -100px;
    }

    .bg-decoration-3 {
      width: 200px;
      height: 200px;
      background: var(--slate-200);
      opacity: 0.4;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
    }

    /* Main container */
    .container {
      position: relative;
      z-index: 1;
      width: 100%;
      height: 100%;
      display: grid;
      grid-template-columns: 1fr 1fr;
      padding: 60px 80px;
      gap: 60px;
    }

    /* Left side - Crystal visualization */
    .crystal-section {
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
    }

    .crystal-container {
      position: relative;
      width: 380px;
      height: 380px;
    }

    /* Background card for crystal */
    .crystal-bg {
      position: absolute;
      inset: 0;
      background: linear-gradient(135deg, var(--crystal-100) 0%, var(--slate-100) 100%);
      border-radius: 24px;
      transform: rotate(-6deg);
      opacity: 0.7;
    }

    .crystal-card {
      position: relative;
      width: 100%;
      height: 100%;
      background: rgba(255, 255, 255, 0.9);
      border: 1px solid var(--slate-200);
      border-radius: 20px;
      box-shadow:
        0 4px 6px -1px rgba(0, 0, 0, 0.05),
        0 10px 15px -3px rgba(0, 0, 0, 0.05),
        0 20px 25px -5px rgba(0, 0, 0, 0.03);
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    /* Crystal SVG */
    .crystal-svg {
      width: 280px;
      height: 280px;
    }

    /* CDL code badge - floating */
    .cdl-badge {
      position: absolute;
      bottom: -16px;
      right: -16px;
      background: white;
      border: 1px solid var(--slate-200);
      border-radius: 12px;
      padding: 12px 20px;
      box-shadow:
        0 4px 6px -1px rgba(0, 0, 0, 0.05),
        0 10px 15px -3px rgba(0, 0, 0, 0.08);
      z-index: 10;
    }

    .cdl-badge code {
      font-family: 'JetBrains Mono', monospace;
      font-size: 15px;
      font-weight: 500;
      color: var(--slate-700);
      letter-spacing: -0.02em;
    }

    .cdl-badge code .system {
      color: var(--crystal-600);
    }

    .cdl-badge code .group {
      color: var(--slate-500);
    }

    .cdl-badge code .miller {
      color: #059669; /* gem-emerald */
    }

    /* Right side - Text content */
    .content-section {
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 24px;
    }

    /* Logo and site name */
    .brand {
      display: flex;
      align-items: center;
      gap: 16px;
    }

    .logo {
      width: 56px;
      height: 56px;
      color: var(--crystal-600);
    }

    .site-name {
      font-size: 42px;
      font-weight: 700;
      color: var(--slate-900);
      letter-spacing: -0.03em;
    }

    /* Tagline */
    .tagline {
      font-size: 28px;
      font-weight: 500;
      line-height: 1.4;
      color: var(--slate-700);
      letter-spacing: -0.01em;
    }

    .tagline-highlight {
      background: linear-gradient(135deg, var(--crystal-600) 0%, #2563eb 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    /* Subtitle */
    .subtitle {
      font-size: 18px;
      color: var(--slate-500);
      line-height: 1.6;
      max-width: 420px;
    }

    /* Feature pills */
    .features {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
      margin-top: 8px;
    }

    .feature-pill {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 8px 16px;
      background: var(--crystal-50);
      border: 1px solid var(--crystal-200);
      border-radius: 9999px;
      font-size: 14px;
      font-weight: 500;
      color: var(--crystal-700);
    }

    .feature-pill svg {
      width: 16px;
      height: 16px;
    }

    /* Bottom URL */
    .url {
      position: absolute;
      bottom: 40px;
      right: 80px;
      font-size: 16px;
      font-weight: 500;
      color: var(--slate-400);
      letter-spacing: -0.01em;
    }
  </style>
</head>
<body>
  <!-- Background decorations -->
  <div class="bg-decoration bg-decoration-1"></div>
  <div class="bg-decoration bg-decoration-2"></div>
  <div class="bg-decoration bg-decoration-3"></div>

  <div class="container">
    <!-- Left: Crystal visualization -->
    <div class="crystal-section">
      <div class="crystal-container">
        <div class="crystal-bg"></div>
        <div class="crystal-card">
          <!-- Diamond octahedron crystal SVG -->
          <svg class="crystal-svg" viewBox="0 0 200 200" fill="none" xmlns="http://www.w3.org/2000/svg">
            <!-- Octahedron crystal structure -->
            <defs>
              <linearGradient id="faceGradient1" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" style="stop-color:#e0f2fe"/>
                <stop offset="100%" style="stop-color:#bae6fd"/>
              </linearGradient>
              <linearGradient id="faceGradient2" x1="100%" y1="0%" x2="0%" y2="100%">
                <stop offset="0%" style="stop-color:#f0f9ff"/>
                <stop offset="100%" style="stop-color:#e0f2fe"/>
              </linearGradient>
              <linearGradient id="faceGradient3" x1="50%" y1="0%" x2="50%" y2="100%">
                <stop offset="0%" style="stop-color:#bae6fd"/>
                <stop offset="100%" style="stop-color:#7dd3fc"/>
              </linearGradient>
            </defs>

            <!-- Back faces (darker) -->
            <polygon points="100,25 160,100 100,175" fill="url(#faceGradient3)" opacity="0.6"/>
            <polygon points="100,25 40,100 100,175" fill="url(#faceGradient2)" opacity="0.7"/>

            <!-- Front faces -->
            <polygon points="100,25 160,100 100,100" fill="url(#faceGradient1)" stroke="#0284c7" stroke-width="1.5"/>
            <polygon points="100,25 40,100 100,100" fill="url(#faceGradient2)" stroke="#0284c7" stroke-width="1.5"/>
            <polygon points="100,175 160,100 100,100" fill="url(#faceGradient3)" stroke="#0284c7" stroke-width="1.5"/>
            <polygon points="100,175 40,100 100,100" fill="url(#faceGradient1)" stroke="#0284c7" stroke-width="1.5"/>

            <!-- Edge highlights -->
            <line x1="100" y1="25" x2="100" y2="175" stroke="#0ea5e9" stroke-width="1" opacity="0.5"/>
            <line x1="40" y1="100" x2="160" y2="100" stroke="#0ea5e9" stroke-width="1" opacity="0.5"/>
          </svg>
        </div>

        <!-- CDL code badge -->
        <div class="cdl-badge">
          <code><span class="system">cubic</span><span class="group">[m3m]</span>:{<span class="miller">111</span>}</code>
        </div>
      </div>
    </div>

    <!-- Right: Text content -->
    <div class="content-section">
      <div class="brand">
        <!-- Logo SVG -->
        <svg class="logo" viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M16 2L28 10V22L16 30L4 22V10L16 2Z" stroke="currentColor" stroke-width="2" fill="#e0f2fe"/>
          <path d="M16 2L16 30" stroke="currentColor" stroke-width="1.5" opacity="0.5"/>
          <path d="M4 10L28 22" stroke="currentColor" stroke-width="1.5" opacity="0.5"/>
          <path d="M28 10L4 22" stroke="currentColor" stroke-width="1.5" opacity="0.5"/>
        </svg>
        <span class="site-name">gemmology.dev</span>
      </div>

      <h1 class="tagline">
        <span class="tagline-highlight">Crystal Structure</span><br>
        Visualization
      </h1>

      <p class="subtitle">
        Professional-grade crystal morphology tools for gemmologists.
        FGA-accurate data with interactive 3D projections.
      </p>

      <div class="features">
        <span class="feature-pill">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
          </svg>
          94+ Minerals
        </span>
        <span class="feature-pill">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/>
          </svg>
          CDL Language
        </span>
        <span class="feature-pill">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>
          </svg>
          FGA Curriculum
        </span>
      </div>
    </div>
  </div>

  <span class="url">gemmology.dev</span>
</body>
</html>
